%Trabalho 3

img = imread('lena_grayscale.png');

img_noise = imnoise(img,'gaussian');

amount = 10;
img_noises = cell(10);

for x = 1:amount
    
    
end


figure;
imshow(img)

figure;
imshow(img_noise)